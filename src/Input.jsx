import React from "react";
import { TextField } from "@mui/material";

export const Input = () => {
  return (
    <TextField
      id="filled-basic"
      label="Ajouter une nouvelle tâche ici"
      variant="filled"
    />
  );
};
